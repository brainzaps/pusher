name: pusher
description: A starting point for Dart libraries or applications.
version: 1.0.0
repository: https://github.com/lollipox/pusher.git

environment:
  sdk: ^3.0.2

# Add regular dependencies here.
dependencies:
  args: ^2.4.2
  yaml: ^3.1.2

dev_dependencies:
  lints: ^2.0.0
  test: ^1.21.0

executables:
  pusher: 
